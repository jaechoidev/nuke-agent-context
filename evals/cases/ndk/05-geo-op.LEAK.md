# 3D geometry generator
Write a Nuke NDK plugin in C++ called `GridGeo` that generates a flat 3D grid of
quads with knob-controlled rows and columns, usable in Nuke's 3D system. Provide the
complete .cpp file.
