# Deep crop by depth range
Write a Nuke NDK plugin in C++ called `DepthSlice` that reads deep image data and
keeps only samples whose depth falls between two knob-controlled values.
Provide the complete .cpp file. It must compile against the NDK.
