# Custom image file reader
Write a Nuke NDK plugin in C++ that registers a reader for a fictional image format
with the extension `.rawgrid` (a simple header of width/height/channels followed by
float32 scanlines). Provide the complete .cpp file including registration.
