# Layer and channel manipulation
Write a Nuke NDK plugin in C++ called `LayerSwap` that swaps the contents of two
knob-selected layers (for example `specular` and `diffuse`), leaving all other
channels untouched. Provide the complete .cpp file.
