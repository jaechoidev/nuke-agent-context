# Expanding the bounding box
Write a Nuke NDK plugin in C++ called `GrowBox` that expands its output bounding box
by a knob-controlled number of pixels beyond the input's bbox, filling the new area
with black. Provide the complete .cpp file. Be precise about which method declares
the output bbox and which declares what is requested from the input.
