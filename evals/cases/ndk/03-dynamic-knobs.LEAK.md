# Knobs created at runtime
Write a Nuke NDK plugin in C++ called `VariableInputs` whose knob panel changes at
runtime: an integer knob sets a count, and that many float knobs appear below it.
Provide the complete .cpp file, including how the panel is rebuilt when the count
changes.
