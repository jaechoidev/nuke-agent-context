# Multi-frame temporal access
Write a Nuke NDK plugin in C++ called `FrameAverage` that averages the current frame
with the frames N before and after it, where N is a knob. Provide the complete .cpp
file, being precise about how frames other than the current one are requested.
